git init
↓
npx create-react-app react-shopping-cart --template typescript
（2分ほどかかる）
↓

npm i @material-ui/core @material-ui/icons
↓

npm i react-query
↓

npm i styled-components @types/styled-components
↓

いらないものを消すApp.css / App.test.tsx / index.css / logo.svg / reportWebVitals.ts / setupTests.ts

↓
index.tsxのいらない文を消す
１．import reportWebVitals from './reportWebVitals';
２．import './index.css';
３．一番下のコメント部分～reportWebVitals(); 
2021/01/21時点ではreact strict modeはconsoleでエラーを吐くので消した
↓

App.tsxのいらない文を消す
上の３つ。import react / import logo / import app.css
app-headerの部分をすべて消す
↓
function( を　アロー関数に
↓
ここからはじまる 動画 5:56~